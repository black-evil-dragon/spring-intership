info:
  name: Create desk
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/v1/desks"
  body:
    type: json
    data: |-
      {
        "name": "Учеба",
        "ownerId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
