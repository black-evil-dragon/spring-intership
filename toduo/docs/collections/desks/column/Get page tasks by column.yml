info:
  name: Get page tasks by column
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/v1/desks/:deskId/columns/:columnId/tasks"
  params:
    - name: columnId
      value: "12"
      type: path
    - name: deskId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
