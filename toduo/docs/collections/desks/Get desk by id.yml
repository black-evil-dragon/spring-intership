info:
  name: Get desk by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/desks/:deskId"
  params:
    - name: deskId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
