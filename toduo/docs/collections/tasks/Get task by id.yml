info:
  name: Get task by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/tasks/:taskId"
  params:
    - name: taskId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
