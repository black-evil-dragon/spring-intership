info:
  name: Create user
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/v1/users"
  body:
    type: json
    data: |-
      {
        "email": "support1@toduo.ru",
        "password": "12341234",
        "firstName": "Ту",
        "lastName": "Ду Листович"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
