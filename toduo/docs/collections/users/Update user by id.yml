info:
  name: Update user by id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/api/v1/users/:userId"
  params:
    - name: userId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "firstName": "Тудуо"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
