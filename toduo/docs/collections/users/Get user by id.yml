info:
  name: Get user by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/users/:userId"
  params:
    - name: userId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
