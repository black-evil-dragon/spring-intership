info:
  name: Get page tasks
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/tasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
