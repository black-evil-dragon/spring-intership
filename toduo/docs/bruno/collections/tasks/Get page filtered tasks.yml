info:
  name: Get page filtered tasks
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/tasks?status=await_work"
  params:
    - name: status
      value: await_work
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
