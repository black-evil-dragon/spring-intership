info:
  name: Update desk by id
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{base_url}}/api/v1/desks/:deskId"
  params:
    - name: deskId
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Практика",
        "ownerId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
