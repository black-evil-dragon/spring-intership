info:
  name: Get page columns by desk id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/desks/:deskId/columns"
  params:
    - name: deskId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
