info:
  name: Delete column by id
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/v1/desks/:deskId/columns/:columnId"
  params:
    - name: columnId
      value: "1"
      type: path
    - name: deskId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
