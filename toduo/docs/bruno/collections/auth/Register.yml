info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "support@toduo.ru",
        "password": "12341234",
        "firstName": "Ivan",
        "lastName": "Ivanov"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
