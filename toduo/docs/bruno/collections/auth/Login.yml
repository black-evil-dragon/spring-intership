info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "support@toduo.ru",
        "password": "12341234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
